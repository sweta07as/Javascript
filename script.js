//JavaScript is a programming language.
//Completely different from Java.
// ./ means current folder.

// One way to run JS code is by creating .html and embedding .js into it. (Proof: Inspect --> Console)
// Another way is to run standalone JS code in two ways :-
    // JS engines such as V8 and SpiderMonkey.
    // node.js and deno are modern implementations that uses JS engines

// ECMAScript (ES) is set of guidelines followed by JS.

// Anything on the computer that runs is called application. 
// It can be running on mobile or web or browser or inside the console or command line app.
console.log("Hello Sweta");